[workspace]
resolver = "2"
members = ["crates/ichormoon-gauntlet/*", "crates/reality-chip/*"]
# Gitaxian Probe is parked: a failed MVP whose source stays in the tree but out
# of the build, so a plain `cargo` resolves the workspace without the flake's
# patched deno_core. Its crates still inherit `*.workspace = true` keys, so they
# do not build on their own either. To bring it back, restore from the commit
# that parked it: this `members` glob as "crates/*/*", the probe-only
# `[workspace.dependencies]`, the size-tuned `[profile.release]`, the
# `[patch.crates-io]` deno_core entry, and flake.nix's V8, Skia, vendored
# deno_core and Android wiring.
exclude = ["crates/gitaxian-probe/*"]

[workspace.metadata.crane]
# crane reads the package name from the root Cargo.toml, which a virtual
# workspace manifest does not have. Without this it warns and uses a placeholder.
name = "ichormoon-gauntlet"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/cramt/progress-engine"

[workspace.dependencies]
anyhow = "1"
facet = { version = "0.46", features = ["nonzero"] }
facet-json = "0.46"
facet-toml = "0.46"
figue = "4"
libm = "0.2"
proptest = { version = "1", default-features = false, features = ["std", "bit-set"] }
rand = "0.9"
rand_chacha = "0.9"
regex = "1"
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
ureq = { version = "3", default-features = false, features = ["rustls"] }
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }

chip-decklist = { path = "crates/reality-chip/decklist" }
chip-scryfall = { path = "crates/reality-chip/scryfall" }
gauntlet-criteria = { path = "crates/ichormoon-gauntlet/criteria" }
gauntlet-toml = { path = "crates/ichormoon-gauntlet/toml" }
gauntlet-sim = { path = "crates/ichormoon-gauntlet/sim" }
chip-stats = { path = "crates/reality-chip/stats" }
