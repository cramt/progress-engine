--- a/uv_compat/tty.rs
+++ b/uv_compat/tty.rs
@@ -397,7 +397,21 @@
     unsafe { __errno_location() }
   }
 
-  #[cfg(not(any(target_os = "macos", target_os = "linux")))]
+  // Android is unix and reaches this module, but `target_os = "android"` is
+  // not `target_os = "linux"`, and bionic spells the accessor `__errno`.
+  #[cfg(target_os = "android")]
+  fn errno_location() -> *mut c_int {
+    unsafe extern "C" {
+      fn __errno() -> *mut c_int;
+    }
+    unsafe { __errno() }
+  }
+
+  #[cfg(not(any(
+    target_os = "macos",
+    target_os = "linux",
+    target_os = "android"
+  )))]
   fn errno_location() -> *mut c_int {
     compile_error!(
       "errno_location not implemented for this platform — \
